# Synthetic crate c0042 (index 42 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0042"
version = "0.1.0"
# license and description intentionally omitted (doctor findings fixture)

[dependencies]
c0012 = { path = "../c0012" }
