# Synthetic crate c0017 (index 17 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0017"
version = "0.1.0"
license = "MIT OR Apache-2.0"
description = "Synthetic dependency-DAG fixture crate c0017 (seed 7)"

[dependencies]
c0003 = { path = "../c0003" }
