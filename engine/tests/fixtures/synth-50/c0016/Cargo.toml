# Synthetic crate c0016 (index 16 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0016"
version = "0.1.0"
# license and description intentionally omitted (doctor findings fixture)

[dependencies]
c0009 = { path = "../c0009" }
