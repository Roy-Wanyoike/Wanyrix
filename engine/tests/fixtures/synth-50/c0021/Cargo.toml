# Synthetic crate c0021 (index 21 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0021"
version = "0.1.0"
license = "MIT OR Apache-2.0"
description = "Synthetic dependency-DAG fixture crate c0021 (seed 7)"

[dependencies]
c0011 = { path = "../c0011" }
