# Synthetic crate c0022 (index 22 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0022"
version = "0.1.0"
# license and description intentionally omitted (doctor findings fixture)

[dependencies]
c0007 = { path = "../c0007" }
