# Synthetic crate c0004 (index 4 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0004"
version = "0.1.0"
license = "MIT OR Apache-2.0"
description = "Synthetic dependency-DAG fixture crate c0004 (seed 7)"

[dependencies]
c0000 = { path = "../c0000" }
