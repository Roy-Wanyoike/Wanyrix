# Synthetic crate c0019 (index 19 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0019"
version = "0.1.0"
# license and description intentionally omitted (doctor findings fixture)

[dependencies]
c0006 = { path = "../c0006" }
