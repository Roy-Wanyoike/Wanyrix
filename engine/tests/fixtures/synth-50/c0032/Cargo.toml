# Synthetic crate c0032 (index 32 of 50, seed 7) — wanyrix synth fixture.
[package]
name = "c0032"
version = "0.1.0"
license = "MIT OR Apache-2.0"
description = "Synthetic dependency-DAG fixture crate c0032 (seed 7)"

[dependencies]
c0030 = { path = "../c0030" }
