/// <reference types="bun-types" />

/**
 * Type surface for the bun test runner (Task 2-a / AUDIT-I4).
 *
 * `bun-types` is already a devDependency; this reference pulls its global and
 * `bun:test` module declarations into the project typecheck without editing
 * tsconfig `types`. The directive must stay on line 1 — triple-slash
 * references are only recognized at the very top of the file.
 */
