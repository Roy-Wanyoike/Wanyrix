# ferrix proposed-change bundle — 1 diff
# generated 2026-09-17T13:42:32.991Z
#
# Each block below is a self-contained unified diff describing a PROPOSAL.
# Ferrix never modifies a repository silently (Gate 19) — review every
# block and apply manually if, and only if, the change is wanted.
# Estimates are estimates: verify via ferrix experiment (Gate 21).
#
# manifest (block ↔ finding cross-reference):
# 1. ferrix-proposals/two-versions-of-arrow-compiled-simultaneously.sh.patch.txt — finding ATL-DEP-003 — target: atlas-ingest
Index: ferrix-proposals/two-versions-of-arrow-compiled-simultaneously.sh.patch.txt
===================================================================
--- /dev/null
+++ ferrix-proposals/two-versions-of-arrow-compiled-simultaneously.sh.patch.txt
@@ -0,0 +1,13 @@
+# ferrix proposed change — apply manually (Gate 19: no silent modification)
+# workspace: atlas-consortium
+# source:   finding
+# target:   atlas-ingest
+# kind:     command
+# finding:  ATL-DEP-003
+# queued:   2026-09-17T13:42:11.427Z
+
+Unify on arrow 54 via `cargo update arrow@53.3.0 --precise 54.2.0` after fixing the parquet import path.
+
+# estimated impact: −4.1s per affected build (estimated — verify via ferrix experiment, Gate 21)
+
+# end of proposal
